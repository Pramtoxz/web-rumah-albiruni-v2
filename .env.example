APP_NAME=Laravel
APP_ENV=local
APP_KEY=base64:lfEfioX0DqR/uTYJ+4MDE9zzzK3hmIQ5ryjJ0rPYluc=
APP_DEBUG=true
APP_URL=http://localhost

# Bypass OTP in development (optional)APP_TIMEZONE=Asia/Jakarta


# Bypass OTP in development (optional)
# Set to true to skip OTP validation in development
# Set to false to test full OTP flow even in development
# If not set, defaults to true when APP_ENV=local or APP_DEBUG=true
BYPASS_OTP_IN_DEV=false

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug
# Set to true to skip OTP validation in development
# Set to false to test full OTP flow even in development
# If not set, defaults to true when APP_ENV=local or APP_DEBUG=true
# BYPASS_OTP_IN_DEV=true

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=schalbiruni
DB_USERNAME=root
DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null
# Session cookie settings
# For production with HTTPS (AWS, aaPanel with SSL):
# - same_site should be 'none' for webview compatibility
# - secure should be true when using HTTPS
# For local development without HTTPS:
# - same_site should be 'lax'
# - secure should be false
SESSION_SAME_SITE=lax
SESSION_SECURE_COOKIE=false
SESSION_HTTP_ONLY=true

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_SCHEME=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"
# WA_GATEWAY_URL=https://wa-gateway.myserverku.web.id
# WA_GATEWAY_SECRET="OkYes!23*&!@%dsjPOETasdk"
# WA_SESSION_NAME=user_1

# WhatsApp notification delay (in seconds) for bulk operations
# Prevents spam and rate limiting during mass notifications
WHATSAPP_NOTIFICATION_DELAY=2

# Firebase Cloud Messaging Configuration
# Path relative to project root (base_path)
FIREBASE_CREDENTIALS=app/Providers/albiruni-pre-school-firebase-adminsdk-fbsvc-9724349ba8.json
FIREBASE_PROJECT_ID=albiruni-pre-school
